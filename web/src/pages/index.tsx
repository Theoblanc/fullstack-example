import type { NextPage } from "next";
import Header from "@components/common/Header/Header";

const Home: NextPage = () => {
  return (
    <div>
      <Header></Header>
    </div>
  );
};

export default Home;
